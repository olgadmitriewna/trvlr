<p>All the <%= continent_adj %> countries</p>

<% @countries.where(region: continent_adj).each do |country| %>
  <%= link_to country_path(country) do %>
  <div class="d-flex">
    <%= image_tag "http://placehold.it/30x30", class: "avatar" %>
    <p><%= country.name %></p>
  </div>
  <% end %>
<% end %>
